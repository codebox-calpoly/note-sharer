{
  "name": "note-sharer",
  "private": true,
  "scripts": {
    "setup": "node setup.mjs",
    "start": "node setup.mjs --skip-supabase",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "format": "npm --prefix frontend run format",
    "gitcheck": "npm --prefix frontend run lint && npm --prefix frontend run test"
  }
}
